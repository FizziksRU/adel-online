# Состояние проекта

Снимок на конец сессии, в которой были прочитаны полные правила из
`ADEL pravila.docx` и движок приведён к ним. Всё, что ниже, проверено запуском,
а не по памяти.

## Коротко

```
npm test      -> SMOKE ✓ RULES ✓ LAYOUT ✓ ENDINGS ✓ SPECIALS ✓ CREW ✓ PLAYTHROUGH ✓ UI ✓
                 520 проверок в восьми файлах, exit 0
npm run build -> ✓ built
npm start     -> HTTP 200, лобби отвечает ["adel"]
```

Работает: движок правил, лобби, мультиплеер, интерфейс, прод-сборка,
персистентность партий. Поддержаны два состава — **АДЕЛЬ + 2** (18 ходов) и
**АДЕЛЬ + 3** (15 ходов).

| Файл | Проверок | О чём |
| --- | --- | --- |
| `rules.js` | 131 | отдельные правила, состав коробки, `playerView` |
| `specials.js` | 89 | семь специальных карт АДЕЛЬ |
| `crew.js` | 109 | экипаж: поиск, тьма, огнетушитель, перегруз, показ, шпионаж, точка невозврата, состав на двоих |
| `playthrough.js` | 51 | пять полностью отыгранных партий (бот в `bot.js`) |
| `ui.js` | 52 | рендер настоящих компонентов интерфейса |
| `endings.js` | 36 | четыре концовки точечно |
| `smoke.js` | 34 | полный цикл хода через настоящий boardgame.io, оба состава |
| `layout.js` | 18 | геометрия схемы корабля |

---

## Что сделано в этой сессии

### Семь специальных карт АДЕЛЬ

Четыре придуманные заглушки заменены на семь настоящих карт с коробки. Все семь
замешиваются в общую колоду (20 локаций + 7 = 27 карт), отдельной зоны
`G.adel.specials` больше нет. Разбор карт — в
[COMPONENTS.md](COMPONENTS.md#колода-адель--27-карт).

Согласованные решения: «Распространение» стоит свои 3⚡ **вместо** консольной
цены (фишка с консоли всё равно снимается);
«Дефрагментация» возвращает фишки **на консоль** и отклоняется целиком, если не
хватает ячеек; «Пересчёт вероятностей» **гасит отмену события**, поставленную
терминалом тревоги.

### Партия на двух членов экипажа

Строки таблицы маркеров для двоих в буклете нет — раскладка задана владельцем
коробки: каждый знает по одному предмету синей и красной миссии, локацию
линзы-сетчатки знают **оба**. Где лежат сами предметы — не знает никто. Ходов
18, карты «Тишина» остаются в колоде (их убирают только вчетвером и впятером).

`CREW_SIZE` больше не жёсткая константа: поддерживаемые составы выводятся из
`MARKER_TABLE`, лобби предлагает выбор на 3 или 4 игроков, клиент собирается
под оба размера стола.

### Правила, приведённые к буклету

Проведён аудит «правила против кода» шестью агентами по разделам, каждая
находка отдельно проверена на опровержение. Из подтверждённых исправлено:

| Было | Стало | Правило |
| --- | --- | --- |
| Тьма дорожала только при выходе | Считается и на входе, и на выходе; две тёмные локации подряд стоят двух лишних кубиков | 8.1 «включая вход и выход» |
| Огнетушитель не тушил пожар в своей локации и жил вечно | Тушит сразу при активации, истрачивается в конце хода | 13.0 «включая текущую» |
| Лишний предмет при перегрузе сбрасывался сам | Сбрасывает игрок ходом `dropItem`; до этого он не может ни действовать, ни завершить ход | 7.0 «на ваш выбор» |
| Показ маркеров переживал расставание | Прекращается при уходе из локации и при выходе в космос | 7.2 «только после новой встречи» |
| Шпионаж звался повторно, превращая 50/50 в 100% | Раз за ход на игрока и только после раскрытия планов | 8.1 |
| Предмет с нулевым зарядом переставал работать на ход раньше | Работает, пока не сброшен в конце следующего хода | 10.1 |
| Дрон применялся дважды за ход | Счётчик снимается в начале хода, а не при завершении действий | 13.0 |
| Заблокированная дверь мешала убрать опасность у соседа | Мешает только движению; соседство считается по проёму | 8.1, «Уточнения» |
| Один флаг на оба терминала тревоги | Два жетона, чинятся поштучно батареей или центральным терминалом | 11.0 |
| Жетоны повреждений не кончались | Их четыре; пятое столкновение жетона не добавляет | состав коробки |
| «Атака» игралась в фазу АДЕЛЬ, после фиксации планов | Играется в окне фазы событий — до планирования | 8.2 |
| Поиск показывал только верхний предмет стопки | Показывает всю стопку, забрать можно любой | 10.0 |
| Осмотр и взятие стоили по кубику каждый | Одно действие: осмотрели — решаете, брать или оставить, бесплатно | 7.0 «ПОИСК» |
| Точка невозврата стояла на месте | Сдвигается каждый ход на 1, с «Дрейфом» — на 2; обогнав жетон хода, закрывает красную миссию | уточнено владельцем коробки |
| АДЕЛЬ приходила к первой фазе с шестью фишками | Раскладочные три и есть запас первого хода | 2.0 шаг 2 |
| Сброс колоды событий хранил названия, а не карты | Хранит карты; при перетасовке снимается отметка об отмене | найдено отыгрышем |
| Цикл выбора стартовой позиции был без ограничителя | Ограничен: вырожденный генератор больше не подвешивает `setup` | серверный риск |

Последний — серверный: `setup` крутился бы вечно, вешая процесс целиком.
Нашёлся через зависший `playthrough.js`.

### Закрытые утечки скрытой информации

К пяти закрытым ранее (планы за ширмами, `redact` на аргументах ходов, колода
событий, стопка лаборатории, `knownItems` при шпионаже) добавились:

6. **Неудачная доставка ключевого предмета** называла в общем журнале предмет и
   локацию — АДЕЛЬ бесплатно узнавала, у кого что и какая локация уже проверена.
   Публично теперь «специальное действие не удалось», подробность уходит в
   личный журнал (`G.privateLog`, в `playerView` отдаётся только своя часть).
7. **Показ предметов и маркеров** сохранялся после расставания.

Каждая закрыта тестом; проверено, что тест падает при откате фикса.

При правках `playerView` держите в голове: фильтрация состояния — не весь
контур. Аргументы ходов идут отдельным каналом и требуют `redact: true`.

### Интерфейс

Панель карт АДЕЛЬ вынесена в `src/client/AdelCards.jsx`: она разрослась до
трети `Board.jsx` и была непроверяемой, потому что сидела за внутренним
`useState`. Теперь выбор приходит пропсом, и каждое состояние пикера покрыто
тестом.

Исправлено: `setSel` во время рендера; интерфейс и движок считают законные цели
**одним кодом** (`hazardCardRules` экспортирован из движка) — предложить
отклоняемый ход стало нельзя структурно; появились переход между секциями
открытого космоса, панель сброса при перегрузе, выбор предмета из стопки,
направление «забрать» у терминала доставки, панель личного журнала, защита от
отсутствующего `myP` (наблюдатель больше не роняет экран), «Атака» через общий
пикер (стали доступны дверь и выбор компьютер/терминал).

Обратная связь об отклонённом ходе: обёртка над `moves` сравнивает объект `G`
до и после — если состояние не сменилось, движок ход отверг, и всплывает
сообщение. Причину клиент вычислить не может (он видит отфильтрованное
состояние), поэтому текст общий, а подробности приходят в личный журнал.

### Сервер

`origins` сужен: по умолчанию только localhost, адрес развёрнутого сервера
задаётся `ALLOWED_ORIGIN`. Раньше стоял предикат «разрешить любой origin».

Партии переживают перезапуск: `FlatFile` из boardgame.io (потребовал
`node-persist`, добавлен в зависимости), каталог задаётся `DB_DIR`, в Dockerfile
объявлен том `/data`. **Проверено вручную:** партия создана, игрок к ней
присоединился, сервер убит и поднят заново — партия открывается с сохранённым
составом.

---

## Правки после живой проверки за столом

Владелец коробки поднял сервер и прошёлся по интерфейсу. Найденное исправлено.

### Запас фишек АДЕЛЬ

К своей первой фазе АДЕЛЬ приходила с **шестью** фишками вместо трёх:
раскладочные три считались добавкой к пополнению первого хода. Теперь они и
есть запас первого хода, пополнение начинается со второго.

### Поиск — одно действие, а не два

Главная находка. По правилам «поиск» — это одно действие: посмотрели жетон и
решаете, брать его или оставить. В интерфейсе стояли две кнопки, и каждая
тратила кубик: осмотр — раз, «забрать» — два. Игрок платил дважды за одно
действие правил.

Теперь: кнопка «Поиск» тратит кубик и открывает выбор «Взять / Оставить на
месте», и этот выбор уже бесплатен — он часть того же действия. Предложение
живёт «здесь и сейчас»: ушёл из локации или закончил ход — оно пропало, а
второй предмет из той же стопки требует нового поиска. Отказ оформлен
отдельным ходом `leaveItem`, чтобы интерфейс мог закрыть предложение, не
тратя кубик и ничего не подбирая.

### Точка невозврата — новое правило

Раньше жетон стоял на месте и двигался только событием «Дрейф». Теперь:

- **каждый ход** точка сдвигается на одно деление;
- на ходу с «Дрейфом» — сразу на два (обычный шаг плюс само событие);
- когда точка обгоняет жетон хода, **красная миссия закрыта**: сбежать уже не
  успеть. Командный терминал отыгрывает деление назад и может открыть её снова.

Следствие, которое стоит держать в голове: жетон хода и точка идут навстречу,
поэтому окно красной миссии закрывается примерно **на 9-м ходу из 18**, а с
«Дрейфами» и раньше. Побег стал гонкой, а не запасным планом. Отыгранная
партия по красной миссии теперь заканчивается ровно на границе — ход 10 при
точке 10, что заодно проверяет засчитывание равенства.

### Интерфейс

- буквы на ячейках консоли заменены значками (🔥 🫁 🌑 🔒 👁 🚪) — те же, что
  на карте корабля, поэтому «что на поле» и «что доступно АДЕЛЬ» читаются
  одинаково;
- маркеры миссий получили обводку цветом своей миссии; линза-сетчатка нужна
  обеим, поэтому у неё рамка из двух цветов;
- в шапке каждой миссии появился счётчик: сколько предметов осталось, сколько
  повреждений на корабле, какой запас по времени; закрытая красная миссия
  помечается «ЗАБЛОКИРОВАНА»;
- названия предметов на схеме больше не режутся до девяти символов.

## Отыгранные партии

`test/playthrough.js` играет партии целиком, настоящими ходами, через настоящий
цикл фаз. АДЕЛЬ в них не бездействует: каждый ход выкладывает фишки картами и
тратит энергию.

**Ключевые предметы лежат на поле лицом вниз, и экипаж обязан их найти.** Это
инвариант, проверяемый в каждой партии: перед первым ходом у всех пустые руки.
Ходит за экипаж бот из `test/bot.js` — обходит корабль, тратит действие
«поиск», забирает ключевые предметы, свои маркеры знает, чужие узнаёт только
при встрече в одной локации, доставляет спец. действием.

| Партия | Состав | Итог | Ходов | Что проверяет по дороге |
| --- | --- | --- | --- | --- |
| 1 | АДЕЛЬ + 2 | победа синей миссии | 13 из 18 | синюю карту находит тот, кто НЕ знает её локации, и доставляет только после встречи с напарником; столкновение кладёт повреждение, попытка активировать топор проваливается, экипаж идёт к ремонтному терминалу и возвращается |
| 2 | АДЕЛЬ + 2 | победа красной миссии | 9 из 18 | побег успевает ровно на границе: ход 10 при точке невозврата 10 |
| 3 | АДЕЛЬ + 2 | победа АДЕЛЬ гибелью | — | пять ран через пожары, проверки духа провалены |
| 4 | АДЕЛЬ + 2 | победа АДЕЛЬ по времени | 18 | никто не погиб |
| 5 | АДЕЛЬ + 3 | победа синей миссии | 7 из 15 | при трёх в экипаже линзу знает только один — сведения размазаны тоньше |

Число ходов в каждой партии зафиксировано проверкой намеренно: это замок на
весь ход партии. Любое послабление в правилах укорачивает партию и ломает
проверку, даже когда победа всё равно случается.

**Бот намеренно не дублирует правила движка.** Он идёт активировать финальный
предмет, как только доставлены три первых, и узнаёт от движка, что условия
миссии не выполнены. Первая версия бота сама проверяла «нет повреждений» и
«хватает времени» — и из-за этого тест переставал замечать, если движок терял
эти правила. Проверено мутациями: после снятия дублирования отыгрыш стал ловить
потерю условия синей миссии и требования трёх доставок.

Отдельно проверено то, о чём вы просили: предметы напарника закрыты, пока
игроки не в одной локации, и снова закрываются, когда разошлись.

## Мутационная проверка

Зелёному прогону с первого раза не доверял: код ломался намеренно, тест обязан
был упасть. Проверено **51 мутация** — цены карт, всё-или-ничего у
«Дефрагментации», гашение отмены события, счёт добора, потолок энергии, вывод
`nextEvent`, ограничение видов фишек, тьма на входе и выходе, пожар и фонарь
против тьмы, огнетушитель, блокировка действий перегрузом, прекращение показа,
предел подглядываний, стопка предметов, запас жетонов повреждений, сокрытие
неудачной доставки, знание линзы обоими, ограничитель в `setup`, защита от
отсутствующего игрока, личный журнал, панель сброса, окно «Атаки», переходы в
космосе, ежеходный сдвиг точки невозврата, «Дрейф» на два деления, блокировка
красной миссии, запас фишек первого хода, бесплатность взятия после осмотра.
Все ловятся.

Две проверки по дороге оказались бутафорскими (слово `disabled` искалось по
всей странице; ограничение видов фишек не изолировалось) — переписаны.

---

## Осталось

### Расхождения с буклетом, оставленные сознательно

- **Все семь специальных карт в игре.** Буклет велит убрать две для первой
  партии; вы решили играть полным набором. Включается константой
  `SPECIALS_REMOVED` в `data.js` — число оставшихся считается от длины массива.
- **«Дефрагментация» возвращает фишки на консоль, а не в мешочек.** Текст самой
  карты говорит «на консоль», раздел 7.0 буклета — «обратно в мешочек».
  Приоритет отдан карте; правится в одном обработчике.
- **Доплату кубиком за тьму и «вредоносную программу» выбирает движок** — берёт
  первый доступный по порядку действий. По правилам кубик выбирает игрок.
  Заметно, только когда у игрока разнородный план.
- **Согласие на «забрать предмет»** терминалом доставки подтверждается голосом,
  кнопка его не спрашивает.

### Не сделано

- Составы на 4–5 членов экипажа: нужен четвёртый планшет персонажа и строки
  таблицы маркеров (в присланных фото они не читаются).
- Сервер не подхватывает правки движка на лету — нужен перезапуск `npm run dev`.
- Деплой: Dockerfile готов, том `/data` объявлен, развёртывание не делалось.
- Переподключение после обрыва связи и мобильная вёрстка.

### Что стоит проверить живой партией

Механики закодированы и покрыты тестами, но за столом не игрались. Смотреть
стоит прежде всего на баланс. Один пункт отсюда уже закрыт живой проверкой:
цена «Распространения» была 3⚡ **сверх** консольной, выходило 8–11⚡ — картой
никто не стал бы пользоваться. По решению владельца коробки платится только
своя цена карты.

Осталось:

1. **Семь спецкарт в колоде из 27**: примерно каждая четвёртая карта в руке
   АДЕЛЬ не даёт выложить фишку. Если темп опасностей просядет — поставьте
   `SPECIALS_REMOVED = 2`.
2. **Тьма, дорожающая и на входе, и на выходе**, читается из правил буквально,
   но делает коридор из двух тёмных локаций очень дорогим. Если ощущается
   перебором — правится в `moveTax`.
